import styles from "../styles";

const Button = () => {
  return (
    <button
      type="button"
      className={`py-4 px-12 bg-blue-gradient font-poppins font-medium text-[18px] text-primary font-bold mt-10 rounded-[10px]`}
    >
      Get Started
    </button>
  );
};

export default Button;
